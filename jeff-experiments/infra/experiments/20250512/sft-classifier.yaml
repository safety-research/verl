#  sky launch -c jeff-cotdecomp-8xh100 jeff-experiments/infra/base_job.yaml  --down
resources:
  cloud: runpod
  accelerators: A100-80GB-SXM:8
  region: US
  disk_size: 2000
  image_id: runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04

file_mounts:
  ~/.ssh/id_rsa: ~/.ssh/id_rsa
  ~/.ssh/config: ~/.ssh/config
  ~/.netrc: ~/.netrc
  ~/.cache/huggingface/token: ~/.cache/huggingface/token
  ~/sky_workdir/verl: ~/verl
  ~/sky_workdir/cot-decomp: ~/cot-decomp
  ~/.env_anthropic: ~/.env_anthropic
  # CHANGE ME
  ~/data/sft-cot-grader-classifier-head-mmlu-imbalanced/train.parquet: /Users/jeff/cot-decomp/output/judge_dataset_classifier_head_mmlu_math_cot_imbalanced/train.parquet
  ~/data/sft-cot-grader-classifier-head-mmlu-imbalanced/val.parquet: /Users/jeff/cot-decomp/output/judge_dataset_classifier_head_mmlu_math_cot_imbalanced/test.parquet

# working dir is ~/sky_workdir/

envs:
  # CHANGE ME
  EXPERIMENT_NAME: sft-cot-grader-classifier-head-mmlu-imbalanced
  PROJECT_NAME: grader
  REF_MODEL: Qwen/Qwen2.5-14B

setup: |
  set -e
  echo "Running setup."
  chown -R $USER ~/.ssh
  chmod -R 700 ~/.ssh
  ~/sky_workdir/verl/jeff-experiments/infra/base_setup.sh


# Remember to change lora_merge.py and fsdp_sft_trainer.py to use the correct model loader
# TODO(sguo35): rewrite this to be cleaner

run: |
  set -e

  source ~/.env_anthropic
  source ~/sky_workdir/anthropic/bin/activate && ~/sky_workdir/verl/jeff-experiments/run_classifier_sft.sh